version: 2
tables:
- table:
    schema: public
    name: accounts
  select_permissions:
  - role: user
    permission:
      columns:
      - name
      - chain
      filter: {}
- table:
    schema: public
    name: api_keys
  select_permissions:
  - role: system
    permission:
      columns:
      - key
      - domains
      - created_at
      - updated_at
      - user_id
      filter: {}
- table:
    schema: public
    name: chaingraph_users
- table:
    schema: public
    name: chains
  select_permissions:
  - role: user
    permission:
      columns:
      - name
      - chain_id
      - rpc_endpoint
      filter: {}
- table:
    schema: public
    name: tables
  select_permissions:
  - role: user
    permission:
      columns:
      - chain
      - code
      - scope
      - primary_key
      - data
      - updated_at
      - created_at
      filter: {}
- table:
    schema: public
    name: transactions
  select_permissions:
  - role: user
    permission:
      columns:
      - chain
      - block_num
      - trx_id
      - data
      filter: {}
