CREATE TABLE "public"."balances"("chain_id" text NOT NULL, "contract" text NOT NULL, "account" text NOT NULL, "balance" text NOT NULL, PRIMARY KEY ("chain_id","contract","account") , FOREIGN KEY ("chain_id", "contract") REFERENCES "public"."tokens"("chain_id", "contract") ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("chain_id", "account") REFERENCES "public"."accounts"("chain_id", "account_name") ON UPDATE restrict ON DELETE restrict);
