CREATE TABLE "public"."tables"("chain_name" text NOT NULL, "block_num" numeric NOT NULL, "block_id" text NOT NULL, "contract" text NOT NULL, "table" text NOT NULL, "scope" text NOT NULL, "primary_key" text NOT NULL, "data" jsonb NOT NULL, "created_at" timetz NOT NULL DEFAULT now(), "updated_at" timetz NOT NULL DEFAULT now(), PRIMARY KEY ("chain_name","block_num","block_id","contract","scope","table","data") , FOREIGN KEY ("chain_name") REFERENCES "public"."chains"("name") ON UPDATE restrict ON DELETE restrict);
