overwrite: true
schema:
  - http://localhost:8088/v1/graphql:
      headers:
         'x-hasura-admin-secret': '=NBc;?2eduh)?mxq'
        #"${HASURA_GRAPHQL_ADMIN_SECRET}"
#  TODO: fix authentication, in the meantime turn of hasura secret to run codegen
documents: ./schema.graphql
generates:
  generated/hasura-graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
    config:
      rawRequest: true
      preResolveTypes: true
  ./graphql.schema.json:
    plugins:
      - 'introspection'
