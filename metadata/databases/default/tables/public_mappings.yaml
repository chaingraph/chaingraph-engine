table:
  name: mappings
  schema: public
select_permissions:
  - role: api_user
    permission:
      columns:
        - abi
        - tables
        - chain
        - contract
        - contract_type
      filter: {}
