table:
  name: table_rows
  schema: public
select_permissions:
  - role: api_user
    permission:
      columns:
        - chain
        - contract
        - table
        - scope
        - primary_key
        - data
      filter: {}
