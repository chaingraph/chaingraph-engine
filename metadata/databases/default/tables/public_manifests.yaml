table:
  name: manifests
  schema: public
select_permissions:
  - role: api_user
    permission:
      columns:
        - app_name
        - description
        - url
        - app_id
      filter: {}
